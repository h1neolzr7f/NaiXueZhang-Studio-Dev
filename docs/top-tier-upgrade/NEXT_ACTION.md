# NEXT_ACTION

Copy-paste for the next human or agent:

> D-020 体验壳已按效果图落到 `cursor/experience-agent-ux-top-tier`：`/desk` + 侧栏/顶栏/右侧专员面板。经典 8 项顶栏仍是关闭助手后的回退。不要发 Release、不要推远程，除非用户明确要求。下一步：挪开本地隔离 `config.json` 后连续两轮全量 pytest，再决定是否写 `EXPERIENCE RC: PASS`。
